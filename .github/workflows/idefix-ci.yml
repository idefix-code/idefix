name: Idefix CIs
run-name: Idefix CIs
on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
  
jobs:
  Hydro:
    runs-on: self-hosted
    steps:
      - name: Check out repo
        uses: actions/checkout@v3
      - name: Run Hydro test
        run: cd test && ./checks_hydro.sh $TEST_OPTIONS
