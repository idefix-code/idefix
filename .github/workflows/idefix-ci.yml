name: Idefix CIs
run-name: Idefix CIs
on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
  
jobs:
  Hydro:
    runs-on: self-hosted
    steps:
      - name: Check out repo
        uses: actions/checkout@v3
      - run: cd test
      - run: ./checks_hydro.sh $TEST_OPTIONS
